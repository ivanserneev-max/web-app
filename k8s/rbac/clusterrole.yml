apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: prometheus
rules:
- apiGroups: ['']          # '' = core API group
  resources: ['pods', 'nodes', 'services', 'endpoints']
  verbs: ['get', 'list', 'watch']  # Глаголы: get,list,watch
