apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - config.yml             # реальный, в .gitignore
  - secret.yml             # реальный, в .gitignore
  - service.yml
  - statefulset.yml
