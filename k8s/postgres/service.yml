apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: monitoring
  labels:
    app.kubernetes.io/name: postgres
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: postgres
  ports:
    - name: http
      port: 5432
      targetPort: 5432
      protocol: TCP
